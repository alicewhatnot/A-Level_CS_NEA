SUBROUTINE tokenize(user_function): 
    token_array = []

    FOR index ← 1 TO LEN(user_function)
        character ← user_function[index]
        IF 64 < character < 91 OR 96 < character < 121 THEN
            token ← 'NAME'
    
        token_array.append((token, character))

#Add Comments!
#Also continue with adding types of token
#And research how regex works
